# ==========================================
# INVENTORY SERVICE - LOCAL CONFIGURATION
# ==========================================
# This file should be MINIMAL - all configs come from Config Server
# Only essential bootstrapping information here

# Application name (must match config server's file name)
spring.application.name=inventory-service

# Config Server Import (Spring Boot 2.4+)
spring.config.import=optional:configserver:http://localhost:8888

# ==========================================
# LOCAL DEVELOPMENT OVERRIDES (Optional)
# Uncomment ONLY when running WITHOUT Config Server
# ==========================================
# server.port=9081
# 
# # Database
# spring.datasource.url=jdbc:mysql://localhost:3306/bookstore_inventory?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=admin
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 
# # JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# 
# # Eureka (disable for local testing)
# eureka.client.enabled=false
# 
# # Logging
# logging.level.root=INFO
# logging.level.com.book.management.inventory=DEBUG